function loadPosts() {          // dentro da função é onde iremos fazer a requisição
    fetch()                    //retorna uma promessa, guardar em uma var e depois req.then para esperar a resposta ou .then para ir já configurando
}